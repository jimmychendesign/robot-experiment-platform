# UI Prompt

Review `docs/design.md` and the components/tokens in `app/design-system/` before changing the interface. Preserve accessibility, keyboard focus, responsive behavior, existing interactions, and semantic status feedback. Prefer existing components and tokens over one-off values.
