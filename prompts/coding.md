# Coding Prompt

Implement the requested change in the active source under `app/`. Preserve unrelated user changes, reuse the local design system, run the smallest relevant checks, and do not create a release unless explicitly requested. Update project/session documentation when the change affects architecture, behavior, or status.
